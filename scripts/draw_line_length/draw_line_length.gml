var xPos = argument0; ///@param x
var yPos = argument1; ///@param y
var length = argument2; ///@param length
var angle = argument3; ///@param angle

draw_line(xPos,yPos,xPos + lengthdir_x(length,angle),yPos + lengthdir_y(length,angle));